import React from "react"; 

function BookReviewForm(){
    return (
        <></>
    );
}
export default BookReviewForm;
 
 
 
 {/* {% if user.is_authenticated %}
                                <div className="ps-3 mt-3">
                                    <form>
                                    <textarea className="form-control form-input" id="customer_review" name="customer_review" rows="4" placeholder="Write your review"></textarea>
                                    <p className="mt-2">How would you rate for this book?</p>
                                    {{ star_rating.rating_for_vote(book.id, 0, 0)}}
                                    <input type="hidden" value="{{book.id}}" id="hdf_book_id" />
                                    <input type="hidden" value="-1" id="hdf_rating_value" />
                                    <input type="button" id="submit-review" className="btn btn-primary mt-2 mb-2 disabled" value="Submit">
                                    <br/>
                                    <span id="book-review-form-message" className="mt-3"></span>
                                    </form>
                                </div>
                                {% else %}
                                <div className="ps-3 mt-3">
                                    <a className="btn btn-outline-primary btn-sm" href="/auth/login"> Login to post a review </a>
                                </div>
                                {% endif %} */}